import { HoverLighlightDirective } from './hover-lighlight.directive';

describe('HoverLighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverLighlightDirective();
    expect(directive).toBeTruthy();
  });
});
